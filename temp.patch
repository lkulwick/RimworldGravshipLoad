*** Begin Patch
*** Update File: Source/Storage/GravloadMapComponent.cs
@@
-        public override void MapComponentTick()
-        {
-            base.MapComponentTick();
-            this.ProcessPendingManagedBuildings();
-        }
-
-        public void NotifyManagedBuildingSpawned(Building_Storage storage)
-        {
-            if (storage == null || storage.Map != this.map)
-            {
-                return;
-            }
-
-            if (!this.pendingManagedBuildings.Contains(storage))
-            {
-                return;
-            }
-
-            this.ProcessPendingManagedBuildings();
-        }
+        public override void MapComponentTick()
+        {
+            base.MapComponentTick();
+            if (this.ProcessPendingManagedBuildings())
+            {
+                this.NotifyCargoWindowsDirty();
+            }
+        }
+
+        public void NotifyManagedBuildingSpawned(Building_Storage storage)
+        {
+            if (storage == null || storage.Map != this.map)
+            {
+                return;
+            }
+
+            if (!this.pendingManagedBuildings.Contains(storage))
+            {
+                return;
+            }
+
+            if (this.ProcessPendingManagedBuildings())
+            {
+                this.NotifyCargoWindowsDirty();
+            }
+        }
*** End Patch
*** Begin Patch`n*** Update File: Source/Storage/GravloadMapComponent.cs`n@@`n         private void RestoreSavedSettings()`n         {`n             this.savedSettings.Clear();`n             if (this.savedSettingsState == null)`n             {`n                 this.savedSettingsState = new List<ManagedParentState>();`n             }`n`n             for (int i = 0; i < this.savedSettingsState.Count; i++)`n             {`n                 ManagedParentState state = this.savedSettingsState[i];`n                 ISlotGroupParent parent = state?.ResolveParent();`n                 if (parent != null && state.Settings != null)`n                 {`n                     this.savedSettings[parent] = state.Settings;`n                 }`n             }`n`n             this.NotifyCargoWindowsDirty();`n         }`n*** End Patch
